query AccountInfo($accountId: Long!) {
    player(steamAccountId: $accountId) {
        matchCount
        winCount
        steamAccount {
            id
            name
            avatar
            isDotaPlusSubscriber
            seasonRank
        }
    }
}
